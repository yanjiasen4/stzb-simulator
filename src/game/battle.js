import Hero from './hero'
import Skill from './skill'

function battle (offense, defense) {
  const testAttr = [1, 1, 1, 0.1, 0.1, 0.1]
  let testHero = new Hero(1, 'test', 'Han', 'test intro', testAttr, 1)
  
}

module.exports = battle
